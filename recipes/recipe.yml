# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: raptor-os
description: Victory Build.
base-image: ghcr.io/ublue-os/bazzite
image-version: latest

modules:
  - type: files
    files:
      - source: system_files/raptor-firstboot.service
        destination: /usr/lib/systemd/system/raptor-firstboot.service
      - source: system_files/raptor-browser-choice.sh
        destination: /usr/bin/raptor-browser-choice.sh

  - type: rpm-ostree
    install:
      - firefox
      - fastfetch
      - plasma-systemmonitor
      - htop
      - bleachbit
      - filelight
      - libreoffice
      - kdenlive
      - gimp
      - inkscape
      - krita
      - darktable
      - audacity
      - vlc
      - obs-studio
      - gwenview
      - kamoso
      - p7zip
      - gamemode
      - goverlay
      - wine
      - winetricks
      - git
      - gh
      - nodejs
      - python3
      - python3-pip
      - gcc
      - make
      - cmake
      - ninja-build
      - meson
      - neovim
      - tmux
      - ripgrep
      - fzf
      - python3-gobject
      - libadwaita
      - podman
      - podman-compose
      - zenity
      - curl
      - wget
      - jq
      - variety
      - kcalc

  - type: default-flatpaks@v1
    notify: true
    system:
      install:
        - com.discordapp.Discord
        - com.heroicgameslauncher.hgl
        - com.vscodium.codium
        - com.github.marhkb.Pods
        - com.calibre_ebook.calibre
        - net.cozic.joplin_desktop
        - com.github.marktext.marktext
        - io.missioncenter.MissionCenter
        - net.davidotek.pupgui2
        - org.blender.Blender
        - com.github.tchx84.Flatseal
        - com.usebottles.bottles
        - com.feaneron.Boatswain
        - org.shotcut.Shotcut

  - type: script
    scripts:
      # 1. KDE theme, profile switcher, RAM optimizer,
      #    app-menu category, desktop entries
      - raptor-hud.sh
      # 2. Performance sysctls, zram, memory, DNS, WiFi, Firefox
      - raptor-performance.sh
      # 3. Writes /usr/lib/systemd/system/raptor-gpu-profile.service
      #    MUST run before the systemd module below
      - raptor-gpu-profile.sh
      # 4. Gaming ulimits, Steam/Lutris config, GPU env vars
      - raptor-gaming.sh
      # 5. Raptor Update Manager GUI — runs after raptor-hud.sh
      #    so the X-RaptorOS menu category already exists
      - raptor-update.sh

  - type: systemd
    system:
      enabled:
        - raptor-firstboot.service
