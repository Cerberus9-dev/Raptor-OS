name: raptor-os
description: Private Tactical Gaming OS.
base-image: ghcr.io/ublue-os/bazzite
image-flavor: main
fedora-version: 40

modules:
  - type: rpm-ostree
    install:
      - brave-browser
      - fastfetch
      - plasma-systemmonitor
      - firewalld

  - type: script
    scripts:
      - raptor-hud.sh
