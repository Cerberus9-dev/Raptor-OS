name: raptor-os
description: Victory Build.
base-image: ghcr.io/ublue-os/bazzite
image-flavor: main
fedora-version: 40

modules:
  - type: rpm-ostree
    install:
      - brave-browser
      - fastfetch
      - plasma-systemmonitor

  - type: script
    scripts:
      - raptor-hud.sh
